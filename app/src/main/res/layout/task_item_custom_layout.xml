<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/tast_item_id"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp">

    <CheckBox
        android:id="@+id/checkBoxTask"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true" />


    <!--DELETE-->
    <!--
        "?attr/selectableItemBackgroundBorderless" :
        Android XML düzen dosyasında bir öğeye tıklayabilmek için arka plan uyglaması için kullanılır
        Ayrıca bir öğenin tıklanabilir bir öğe için kenarlarında herhangi bir sınır olmaksızın dokunma geri bildirimi sağlar
        -->
    <Button
        android:id="@+id/buttonDeleteTask"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:text="@string/todo_delete" />

    <!--UPDATE-->
    <Button
        android:id="@+id/buttonUpdateTask"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_toStartOf="@id/buttonDeleteTask"
        android:layout_centerVertical="true"
        android:background="?attr/selectableItemBackgroundBorderless"
        android:text="@string/todo_update" />
</RelativeLayout>